language: python

python:
  - 2.7
  - 3.5

build:
  ci:
    - mkdir -p shippable/testresults
    - pip install 'tox>=1.8'
    - tox -epy${SHIPPABLE_PYTHON_VERSION//./} -- --with-xunit --xunit-file=shippable/testresults/nosetests.xml
